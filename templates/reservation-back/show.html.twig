{% extends 'back.html.twig' %}

{% block title %}Reservation{% endblock %}

{% block body %}
    <h1>Reservation</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ reservation.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('reservation_index_back') }}">back to list</a>

    <a href="{{ path('reservation_edit_back', {'id': reservation.id}) }}">edit</a>

    {{ include('reservation-back/_delete_form.html.twig') }}
{% endblock %}
