{% extends 'back.html.twig' %}

{% block title %}Reclamation{% endblock %}

{% block body %}
<div class="row ">
              <div class="col-12 grid-margin">
                <div class="card">
                  <div class="card-body">
                    <h4 class="card-title"> Reclamation</h4>
                    <div class="table-responsive">
                      <table class="table">
    
        <div class="col-md-6 mb-5 mb-md-0" style="margin: 0 auto;" >
                

     <table class="table table table-borderless  table-striped table-primary table-hover"> 
        <tbody>
        <tr >
                <th>Username</th>
                <td>{{ reclamation.username.nom }}</td>
            </tr>
            <tr>
                <th>Id reclamation</th>
                <td>{{ reclamation.id }}</td>
            </tr>
           
            <tr>
                <th>Msg</th>
                <td>{{ reclamation.msg }}</td>
            </tr>
             <tr>
                <th>Type</th>
             <td>{{ reclamation.type }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ reclamation.dater ? reclamation.dater|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('reclamation_index_back') }}">liste des Reclamations</a>

    <a href="{{ path('reclamation_edit_back', {'id': reclamation.id}) }}">Modifier Reclamation</a>

    {{ include('reclamation-back/_delete_form.html.twig') }}
    
    </div>
                           </div>
</div>
    </tbody>
                      </table>
                    </div>
                  </div>
                </div>
              </div>
            </div>
{% endblock %}
