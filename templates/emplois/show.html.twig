{% extends 'back.html.twig' %}
{% block title %}Emplois{% endblock %}

{% block body %}
<section id="user" class="user">
    
        <div class="content-wrapper">
    <h1>Emplois</h1>

    <table class="table" style="color: white">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ emploi.id }}</td>
            </tr>
            <tr>
                <th>Ddebut</th>
                <td>{{ emploi.Ddebut ? emploi.Ddebut|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Dfin</th>
                <td>{{ emploi.Dfin ? emploi.Dfin|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('emplois_index') }}">back to list</a>

    <a href="{{ path('emplois_edit', {'id': emploi.id}) }}">edit</a>

    {{ include('emplois/_delete_form.html.twig') }}
    </div>
</section><!-- End Contact Section -->
{% endblock %}
