<form method="post" action="{{ path('finance_delete', {'id': finance.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ finance.id) }}">
    <button class="btn btn-primary "> supprimer</button>
</form>
