{% extends 'back.html.twig' %}

{% block title %}Finance{% endblock %}

{% block body %}

    <h1>Finance</h1>
 <div class="row ">
              <div class="col-12 grid-margin">
                <div class="card">
                  <div class="card-body">
                    <h4 class="card-title"> liste des Bilan Financeiére</h4>
                    <div class="table-responsive">
                      <table class="table">
    
        <div class="col-md-6 mb-5 mb-md-0" style="margin: 0 auto;" >     

     <table class="table table table-borderless  table-striped table-primary table-hover"> 
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ finance.id }}</td>
            </tr>
            <tr>
                <th>Totale évenements</th>
                <td>{{ finance.evenement.billet }}</td>
            </tr>
            <tr>
                <th>Totale Stock </th>
                <td>{{ finance.commande.total }}</td>
            </tr>
              <tr>
                <th>Totale Dons </th>
                <td>{{ finance.dons.montant }}</td>
            </tr>

            <tr>
                <th>Totale</th>
                <td>{{ finance.somme }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ finance.date ? finance.date|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('finance_index') }}">liste des Bilans </a>

    <a href="{{ path('finance_edit', {'id': finance.id}) }}">Modifier un Bilan </a>

    {{ include('finance/_delete_form.html.twig') }}
     </div>
                           </div>
</div>
    </tbody>
                      </table>
                    </div>
                  </div>
                </div>
              </div>
            </div>
{% endblock %}
