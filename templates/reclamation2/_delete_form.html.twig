<form method="post" action="{{ path('reclamation2_delete', {'id': reclamation2.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ reclamation2.id) }}">
    <button class="btn">Delete</button>
</form>
