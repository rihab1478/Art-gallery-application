{% extends 'base.html.twig' %}

{% block title %}Reclamation2{% endblock %}

{% block body %}
    <h1>Reclamation2</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ reclamation2.id }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ reclamation2.type }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ reclamation2.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('reclamation2_index') }}">back to list</a>

    <a href="{{ path('reclamation2_edit', {'id': reclamation2.id}) }}">edit</a>

    {{ include('reclamation2/_delete_form.html.twig') }}
{% endblock %}
