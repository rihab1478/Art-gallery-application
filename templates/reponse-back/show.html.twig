{% extends 'back.html.twig' %}

{% block title %}Reponse{% endblock %}

{% block body %}
   <div class="row ">
              <div class="col-12 grid-margin">
                <div class="card">
                  <div class="card-body">
                    <h4 class="card-title"> Réponse</h4>
                    <div class="table-responsive">
                      <table class="table">
    
        <div class="col-md-6 mb-5 mb-md-0" style="margin: 0 auto;" >
                

     <table class="table table table-borderless  table-striped table-primary table-hover"> 
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ reponse.id }}</td>
            </tr>
             <tr>
                <th>reclamation</th>
                <td>{{ reponse.reclamation.msg }}</td>
            </tr>
            <tr>
                <th>Rep</th>
                <td>{{ reponse.rep }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('reponse_index_back') }}">Liste des Réponses</a>

    <a href="{{ path('reponse_edit_back', {'id': reponse.id}) }}">Modifier Réponse</a>

    {{ include('reponse-back/_delete_form.html.twig') }}
    </div>
                           </div>
</div>
    </tbody>
                      </table>
                    </div>
                  </div>
                </div>
              </div>
            </div>
{% endblock %}
