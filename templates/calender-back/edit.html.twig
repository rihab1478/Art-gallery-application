{% extends 'back.html.twig' %}

{% block title %}Edit Calender{% endblock %}

{% block body %}
   <div class="row ">
              <div class="col-12 grid-margin">
                <div class="card">
                  <div class="card-body">
                    <h4 class="card-title">Modifier plan</h4>
                     <div class="team-content-area text-left  wow fadeInUp"  >
        {% for message in app.flashes('success') %}
            <div class="alert alert-success" >
                {{ message }}
            </div>
            {% endfor %}
                    <div class="table-responsive">
                      <table class="table">
   
{% form_theme form 'bootstrap_4_layout.html.twig' %}

                                   {{ form_start(form, {attr: {novalidate: 'novalidate'}}) }}

    
    {{ form_widget(form) }}


{{ form_end(form) }}
 

    <a href="{{ path('calender_index_back') }}">Mes Plans</a>

    {{ include('calender-back/_delete_form.html.twig') }}
  
    
                      </table>
                    </div>
                  </div>
                </div>
              </div>
            </div>
{% endblock %}
