{% extends 'front.office.html.twig' %}
{% block title %}Edit Calender{% endblock %}

{% block body %}
    <h1>Edit Calender</h1>
                                <div class="col-12 col-md-6" style="margin: 0 auto;" >
        
                                  <div class="team-content-area text-left mb-30 wow fadeInUp" data-wow-delay="300ms">
{% form_theme form 'bootstrap_4_layout.html.twig' %}

                                   {{ form_start(form, {attr: {novalidate: 'novalidate'}}) }}

    
    {{ form_widget(form) }}


{{ form_end(form) }}
 

    <a href="{{ path('calender_index') }}">Mes Plans</a>

    {{ include('calender/_delete_form.html.twig') }}
    </div></div>
{% endblock %}
